if [ "$1" = 0 ] ; then
    # if this is uninstallation as opposed to upgrade, delete the service
    /sbin/service routing-engine stop > /dev/null 2>&1
    /sbin/chkconfig --del routing-engine
fi
exit 0